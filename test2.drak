def mod(a, b) {
    if a < b {
        return a;
    }

    return mod(a - b, b);
}

def print(n) {
    if n < 10 {
        print_char(48 + n);
        return 0;
    }

    print_char(48 + mod(n, 10));
    print(n / 10);
}

def main() {
    print(432);
    return 32;
}